{
  "name": "lifehappens-books",
  "private": true,
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "deploy": "npm run build && wrangler pages deploy public --project-name=books",
    "pages:dev": "npm run build && wrangler pages dev public"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  },
  "dependencies": {
    "firebase": "^11.0.2"
  }
}
