services:
  book-site:
    build: .
    container_name: life-moments-book
    restart: unless-stopped
    ports:
      - "4410:8080"
    environment:
      - ACCESS_CODE=moments
      - SECRET_KEY=change-me-life-moments-2026
      - STREAM_SECRET=stream-life-moments-2026
      - TOKEN_TTL=7200
